package notifier;

/**
 * Sends notifications via Slack.
 *
 * TODO: Implement this class
 * - Add a private field to store the channel name
 * - Add a constructor that takes the channel name as a parameter
 * - Implement send() to print: [SLACK] Posting to #{channel}: {message}
 */
public class SlackNotifier implements Notifier {

    // TODO: Add private field for channel name

    // TODO: Add constructor that takes channel name

    @Override
    public void send(String message) {
        // TODO: Print message in format: [SLACK] Posting to #{channel}: {message}
    }
}
